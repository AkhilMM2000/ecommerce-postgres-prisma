# Dependencies
node_modules

# Build output
dist

# Environment variables — never commit secrets
.env

# Logs
npm-debug.log
*.log

# Prisma generated client — regenerated by `prisma generate` (via postinstall)
# Never commit this — each developer regenerates it locally after npm install
src/generated

# TypeScript incremental build cache
*.tsbuildinfo

# OS-generated metadata files
.DS_Store
Thumbs.db

# Editor settings — personal preference, not project config
.vscode/
.idea/

# Test coverage output (for when tests are added later)
coverage/